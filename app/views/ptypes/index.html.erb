<p id='notice'><%= notice %></p>

<h1>Billettyper</h1>

<table>
  <thead>
    <tr>
      <th>Navn</th>
      <th>Pris</th>
      <th>Fødselsdag</th>
      <th colspan='3'></th>
    </tr>
  </thead>

  <tbody>
    <% @ptypes.each do |ptype| %>
      <tr>
        <td><%= ptype.name %></td>
        <td><%= ptype.price %></td>
        <td><%= check_box_tag 'hest', 'ko', ptype.datereq, disabled: true %></td>
        <td><%= link_to 'Vis', ptype %></td>
        <td><%= link_to 'Rediger', edit_ptype_path(ptype) %></td>
        <td><%= link_to 'Slet', ptype, method: :delete, data: { confirm: 'Er du sikker?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Ny Billettype', new_ptype_path %>
