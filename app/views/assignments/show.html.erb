<p id="notice"><%= notice %></p>

<p>
  <strong>Deltager:</strong>
  <%= @assignment.person.name %>
</p>

<p>
  <strong>Opgave:</strong>
  <%= @assignment.task.name %>
</p>

<%= link_to 'Rediger', edit_assignment_path(@assignment) %> |
<%= link_to 'Oversigt', assignments_path %>
